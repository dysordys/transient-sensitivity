# transient-sensitivity
Computer code for the manuscript "Parameter sensitivity of transient community dynamics"

The `code` directory contains Mathematica scripts for automating transient sensitivity analyses. The file `transient_sensitivity_analysis.m` is a package of routines implementing the elements of the analysis. In turn, `examples.nb` is a Mathematica notebook demonstrating how the package can be used, on three different examples.
